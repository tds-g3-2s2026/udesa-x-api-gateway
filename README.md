# udesa-x-api-gateway

Microservicio backend encargado del ruteo interno de requests hacia users-api y posts-api.

Este `main` arranca vacío a propósito, como base limpia para la primera PR. El contenido real
del servicio llega vía Pull Request desde `chore-api-gateway`.
